# Server Configuration
PORT=5001
JWT_SECRET=<CHANGE-ME-USE-A-RANDOM-SECRET-KEY>
JWT_EXPIRES_IN=7d
NODE_ENV=development

# Base URL of the public site (used to build share link URLs).
PUBLIC_SITE_URL=https://your-domain.com
